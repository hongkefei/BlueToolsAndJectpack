<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
   >


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/myrecycle"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:ignore="MissingConstraints" />


</androidx.constraintlayout.widget.ConstraintLayout>
<!--
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".MainActivity">
&lt;!&ndash;第二步骤，生成xml文件&ndash;&gt;
    <data>
        &lt;!&ndash;设置数据源&ndash;&gt;
        <variable
            name="viewmodel"
            type="com.wf.tkt.MyViewModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >
        &lt;!&ndash;第四步：在xml文件中，引用数据源&ndash;&gt;
        <Button
            android:id="@+id/submit_text"
            android:text="修改文字"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:ignore="MissingConstraints" />
        <TextView
            android:id="@+id/updata_text"
            android:textColor="#000"
            android:textSize="40sp"
            android:background="#0f0"
            android:onClick="@{()->viewmodel.textChanged()}"
            android:text="@{viewmodel.userBean.name}"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:ignore="MissingConstraints" />
        <TextView
            android:id="@+id/t_age"
            app:layout_constraintTop_toBottomOf="@id/updata_text"
            android:text="@{viewmodel.userBean.age+viewmodel.userBean.sex}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:ignore="MissingConstraints" />

        <TextView
            android:id="@+id/t_sex"
            app:layout_constraintTop_toBottomOf="@id/t_age"
            android:text="@{viewmodel.userBean.sex}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:ignore="MissingConstraints" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
-->
